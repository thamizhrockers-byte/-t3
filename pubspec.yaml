name: money_tracker
description: Private single-user money tracker with Google Sheets API
publish_to: "none"
environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0
  fl_chart: ^0.69.0
flutter:
  uses-material-design: true
dev_dependencies:
  flutter_lints: ^6.0.0
